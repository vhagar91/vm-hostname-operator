#! This file is auto-generated by `make supervisor-crd-sync`.
#! Source: supervisor-service/config/deployment.yml CRD section. Do not edit by hand.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: hostnamecounters.hostname.vcf.vmware.com
  labels:
    app.kubernetes.io/name: hostname-operator
    app.kubernetes.io/managed-by: supervisor-service
spec:
  group: hostname.vcf.vmware.com
  names:
    kind: HostnameCounter
    listKind: HostnameCounterList
    plural: hostnamecounters
    singular: hostnamecounter
    shortNames:
      - hnc
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required:
                - template
              properties:
                template:
                  type: string
                  description: "Hostname template pattern (e.g., 'vm-###' where # is a digit placeholder)"
                currentIndex:
                  type: integer
                  minimum: 0
                  default: 0
                  description: "Current index for the next hostname generation"
                namespaceScope:
                  type: string
                  description: "Namespace this counter applies to (empty = cluster-wide)"
                locked:
                  type: boolean
                  default: false
                  description: "Lock prevents concurrent counter updates"
                lockedAt:
                  type: string
                  format: date-time
                  description: "Timestamp when the counter was locked"
            status:
              type: object
              properties:
                lastGenerated:
                  type: string
                  format: date-time
                  description: "Timestamp of last hostname generation"
                lastHostname:
                  type: string
                  description: "Last hostname generated"
                generationCount:
                  type: integer
                  description: "Total number of hostnames generated"
      additionalPrinterColumns:
        - name: Template
          type: string
          jsonPath: .spec.template
        - name: Current Index
          type: integer
          jsonPath: .spec.currentIndex
        - name: Namespace
          type: string
          jsonPath: .spec.namespaceScope