# This file is retained as a placeholder. The VCF9 Supervisor Service
# PackageMetadata and Package source files are located in:
#
#   config/supervisor-service/package/package-metadata.yaml
#   config/supervisor-service/package/package.yaml
#
# The upload-ready YAML (PackageMetadata + Package) is generated by:
#
#   make supervisor-service-yaml
#
# See Makefile for details.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hostname-operator-package-info
  namespace: default
data:
  source: config/supervisor-service/package/
  generate: make supervisor-service-yaml